@model IEnumerable<MIS4200Team6.Models.CoreValueLeaderBoard>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Registrar.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Centric)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Comment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TodaysDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Registrar.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Centric)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Comment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TodaysDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.leaderboardID }) |
            @Html.ActionLink("Details", "Details", new { id=item.leaderboardID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.leaderboardID })
        </td>
    </tr>
}

</table>
